name: Match Reminders (every 10min) - DISABLED

# This workflow has been disabled to stop recurring notifications.
# The schedule has been removed to prevent it from running.

on:
  # schedule:
  #   - cron: '*/10 * * * *' # DISABLED - every 10 minutes (UTC)
  workflow_dispatch: {} # Manual trigger only

jobs:
  call-reminder:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "This workflow has been disabled. No action will be taken."
          exit 0
